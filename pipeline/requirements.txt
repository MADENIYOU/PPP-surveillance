paho-mqtt>=1.6,<2.0
pydantic>=2.5
influxdb-client[ciso]>=1.40
psycopg2-binary>=2.9
structlog>=24.1
PyYAML>=6.0
# Workers calibration / anomaly detection
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3
# Prefect flows
prefect>=3.0
# Spatial (kriging)
scipy>=1.12
# Orchestration locale des flows
APScheduler>=3.10
# Superviseur de processus (workers permanents)
supervisor>=4.2
